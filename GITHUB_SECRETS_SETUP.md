# GitHub Secrets

To make the CI pipeline pass without failures, some pipeline components are using public environment variables.
For any real deployment, you must set these inside your Repository > Settings > Secrets and variables > Actions.
