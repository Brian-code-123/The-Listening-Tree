# ---------------------------------------------------------------------------
# The Listening Tree — Local Development Dependencies
#
# Includes Vosk for offline English speech-to-text (requires native
# binary).  For production deployment use requirements.txt instead.
#
# Install:
#   pip install -r requirements-local.txt
# ---------------------------------------------------------------------------

-r requirements.txt
vosk==0.3.45
