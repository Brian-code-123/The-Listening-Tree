# The Listening Tree — Production Dependencies
# FastAPI + PostgreSQL + Zhipu AI

fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
jinja2==3.1.6
itsdangerous==2.2.0
httpx==0.28.1
Authlib==1.3.2
email_validator==2.2.0
gunicorn==23.0.0
python-dotenv==1.0.0
asyncpg==0.30.0
playwright==1.50.0
SpeechRecognition==3.10.4
azure-communication-email==1.0.0

# Migration tooling — CLI/CI-only, never imported by the running app (not in
# api/requirements.txt, which is Vercel's runtime manifest). psycopg2 (sync)
# is deliberate here instead of asyncpg — see alembic/env.py's comment.
alembic==1.13.3
SQLAlchemy==2.0.34
psycopg2-binary==2.9.11
